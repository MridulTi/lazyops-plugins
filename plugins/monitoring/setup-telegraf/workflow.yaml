id: setup-telegraf

name: "Setup Telegraf"

description: "Install telegraf on servers from a config file. Required env: SSH_KEY, AWS_REGION or REGION."

runtime: bash

entrypoint: script.sh

pack: monitoring

version: 1.0.0

inputs:
  - name: servers-file
    required: true
