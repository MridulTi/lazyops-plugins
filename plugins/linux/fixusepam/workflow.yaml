id: fixusepam

name: "Fixusepam"

description: "Fixusepam"

runtime: bash

entrypoint: script.sh

pack: linux

version: 1.0.0

inputs:
  - name: ssh-key
    required: true
  - name: ip1
    required: true
