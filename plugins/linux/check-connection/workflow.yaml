id: check-connection

name: "Check Connection"

description: "Check Connection"

runtime: bash

entrypoint: script.sh

pack: linux

version: 1.0.0

inputs:
  - name: ssh-key
    required: true
  - name: username
    required: true
  - name: ip1
    required: true
