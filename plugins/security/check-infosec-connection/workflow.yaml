id: check-infosec-connection

name: "Check Infosec Connection"

description: "Check Infosec Connection"

runtime: bash

entrypoint: script.sh

pack: security

version: 1.0.0

inputs:
  - name: ssh-key
    required: true
  - name: username
    required: true
