id: repair-qualys-from-s3

name: "Repair Qualys From S3"

description: "Repair/reinstall Qualys agent from S3 or local packages. Required env: S3_BUCKET_PATH, AWS_REGION, SSH_KEY_DIR, QUALYS_LOCAL_PKG_DIR (optional)."

runtime: python

entrypoint: script.py

pack: security

version: 1.0.0

inputs:
  - name: config
    required: true
