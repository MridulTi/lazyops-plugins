id: install-cortex-from-local

name: "Install Cortex From Local"

description: "Install Cortex agent from local packages via SSH. Required env: config file path as arg, AWS_REGION, SSH_KEY_DIR."

runtime: python

entrypoint: script.py

pack: security

version: 1.0.0

inputs:
  - name: config
    required: true
