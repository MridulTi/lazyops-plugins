id: delete-peering-route

name: "Delete Peering Route"

description: "Clean peering route tables with JSON backup. DRY_RUN defaults to true. Required env: AWS_REGION or REGION, DRY_RUN (default: true)."

runtime: python

entrypoint: script.py

pack: aws

version: 1.0.0

inputs:
  - name: peering-file
    required: true
