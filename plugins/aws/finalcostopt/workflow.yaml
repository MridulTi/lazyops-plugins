id: finalcostopt

name: "Final Cost Opt"

description: "Cost optimization analysis for AWS resources. Required env: AWS_REGION (or pass region as arg)."

runtime: python

entrypoint: script.py

pack: aws

version: 1.0.0

inputs:
  - name: region
