id: checkunusedvolumes

name: "Check Unused Volumes"

description: "Audit unused EBS volumes, AMIs, snapshots, and security groups. Required env: AWS_REGION or REGION."

runtime: bash

entrypoint: script.sh

pack: aws

version: 1.0.0
