id: ssh-ssm

name: "Ssh Ssm"

description: "Ssh Ssm"

runtime: bash

entrypoint: script.sh

pack: aws

version: 1.0.0

inputs:
  - name: ips-file
    required: true
  - name: config-file
    required: true
